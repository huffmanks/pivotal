version: 2

project_name: pivotal

before:
  hooks:
    - just clean
    - go mod tidy

builds:
  - id: "pivotal"
    binary: "pivotal"
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
    goarch:
      - amd64
      - arm64
    main: ./cmd/server
    ldflags:
      - -s -w

archives:
  - id: "archive"
    ids:
      - pivotal
    formats: ["tar.gz"]
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    files:
      - README.md
      - LICENSE*

changelog:
  sort: asc
  use: github
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"
      - "merge"

release:
  github:
    owner: huffmanks
    name: pivotal
  draft: false
  prerelease: auto
  footer: >-
    ---
    Built with ❤️ using GoReleaser.
